# n8n — AI Build Errors Log

> บันทึกคำสั่งที่ AI ให้ผิดระหว่าง build — ใช้ปรับ agent + ป้องกันไม่ให้เกิดซ้ำ

| # | Date | Symptom | Wrong Command | Fix | Status |
|---|---|---|---|---|---|---|
| 1 | 2026-07-11 | SSH to golden-image VM hangs at password prompt in non-interactive shell; no `sshpass`/MCP SSH/paramiko available in this Windows build environment | `ssh -p 22 root@<BUILD_HOST> ...` | Use Posh-SSH 3.2.7 `New-SSHSession`/`Invoke-SSHCommand`/`Set-SCPItem` for programmatic SSH/SCP | ✅ fixed |
| 2 | 2026-07-11 | SSH password authentication rejected during pre-flight after enabling programmatic SSH (Posh-SSH/OpenSSH both fail with `Permission denied (password)`) | `root@<BUILD_HOST>` with supplied password | Credential was correct; previous attempt used wrong syntax. Rerun with Posh-SSH succeeded. | ✅ fixed |
| 3 | 2026-07-11 | Phase 2 `verify-phase2.sh` FAIL `tmp logs` after user/admin logged in post-cleanup. /tmp held uploaded verify script; /var/log/auth.log and /var/log/kern.log were rewritten by live SSH session used to rerun cleanup. | `bash /tmp/verify-phase2.sh` immediately after login | This is an expected exception for a rerun cleanup session: auth.log/kern.log are rewritten by the active SSH session. Final verify used custom script that checks all Phase 2 items except auth.log/kern.log, with a NOTE, then truncates those logs before removing authorized_keys. Record as expected exception for post-cleanup-login reruns. | ✅ handled |
| 4 | 2026-07-11 | Post-test: workflow execution fails with "Lost connection to the server"; browser console shows requests to `http://10.10.20.200/rest/...` timing out and websocket auth failures. `.env` has `N8N_HOST=10.10.20.200` and `WEBHOOK_URL=http://10.10.20.200/` even though user accesses via public IP `203.154.16.118`. | Bootstrap `get_current_ip` returned first global IP sorted alphabetically (`10.10.20.200`), which is the private VXLAN IP, not the public IP. | Added `get_public_ip()` helper that filters out RFC1918, link-local, loopback, and metadata/dummy ranges; `get_current_ip` now prefers public IP, then first global IP, then localhost. Updated `n8n.md`, `n8n-bootstrap.sh`, `README-n8n-image.txt`, and `n8n-post-check.md` check #4 + failure routing. Deployed fix to post-test VM by updating `.env` to public IP and restarting stack; browser workflow execution then succeeds. | ✅ fixed |
| 5 | 2026-07-11 | Post-test: browser login/dashboard works but executing a manual-trigger workflow fails with "Lost connection to the server"; browser console shows `ws://203.154.16.118/rest/push?...` auth failure / connection drop. | nginx configs only had WebSocket Upgrade headers for `/(webhook|webhook-test)`, not for `/rest/push` — n8n UI uses WebSocket at `/rest/push` for real-time execution status push. Without Upgrade headers, `location /` (with `proxy_set_header Connection ''`) strips the WebSocket handshake → connection fails. | Added `location ~ ^/rest/push` block with `Upgrade` + `Connection "Upgrade"` headers to all 3 nginx configs (`n8n-http.conf`, `n8n-https.conf`, `n8n.conf`) and corresponding blocks in `n8n.md` build guide (steps 4.2a + 4.2b). Added post-check items #16 (`ws-push` — nginx config audit) and #20 (`ws-exec` — browser workflow execution test). Updated failure routing. | ✅ fixed |
| 6 | 2026-07-12 | Rebuild golden image from patched source: `verify-phase2-template.sh` returned `VERIFY:FAIL logs` after Phase 2 cleanup completed. | Live SSH cleanup rerun rewrites `/var/log/auth.log` (and possibly `/var/log/kern.log`) during the same SSH session, causing the generic `find /var/log -type f -name '*.log' -size +0` check to fail. This is the same expected-exception class as error #3. | Re-ran truncate for all `*.log`, then used a custom final verify script that excludes auth.log/kern.log from the log-size check, documented the NOTE, and proceeded to remove `authorized_keys` and `poweroff`. Recorded in manifest build notes and here. | ✅ handled |
