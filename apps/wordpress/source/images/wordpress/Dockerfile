# PINNED SEMVER TAG — verify digest during build and record in manifest.
FROM wordpress:7.0.0-php8.3-fpm

RUN apt-get update \
 && DEBIAN_FRONTEND=noninteractive apt-get install -y bash bash-completion nano vim-tiny
